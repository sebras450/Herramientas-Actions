name: Flujo Base

on: 
  workflow_dispatch:

jobs: 
  Compilacion: 
    runs-on: ubuntu-latest
    steps:
      - name: Acción_1
        run: | 
          echo "Acciones por parte de la acción 1"


  Instlacion:
    runs-on: ubuntu-latest
    needs: Compilacion
    steps:
      - name: Acción_2
        run: | 
          echo "Acciones por parte de la acción 2"
